1. images are  executable conf file  => images stored in like gocker hub website. we can download images from there and convert in to containers  

2. convert image to docker container
   docker run <inage name>
   docker run -it <image name >  (it is enteractive mode ,open in command promt)

   docker stop <container id >  (to stop container)

3. we can do development in container

4. for linux command  we can download ubuntu image , convert it in to container and run in itnteractive mode 
   you can prctice command there
